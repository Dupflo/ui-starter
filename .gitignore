node_modules/
.env.local
.DS_Store
.next/
dist/
*.tsbuildinfo

# s15-gallery-feedback — working files dropped by the recette feedback tool
# (screenshots + notes on the running /ui page), not repo content.
annotations.md
annotations/
