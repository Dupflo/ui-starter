# Review — Story s08-i18n

Branch: `feature/s08-i18n` @ `2668497` · Diff: `git diff main...feature/s08-i18n`
Reviewer: anti-hallucination fresh-context pass.

## Gate (run by the reviewer, Node 22 via nvm)

| Check                 | Result                                                                                           |
| --------------------- | ------------------------------------------------------------------------------------------------ |
| `npm run typecheck`   | pass (tsc --noEmit, clean)                                                                       |
| `npm run lint`        | pass — 0 errors, 4 warnings (pre-existing, `components/ui/modal.tsx`, not touched by this story) |
| `npm run lint:design` | pass — no arbitrary values in 86 files (12 allowlisted)                                          |
| `npm run test`        | pass — 18 files, 117 tests                                                                       |
| `npm run build`       | pass — includes `check-design-tokens` prebuild                                                   |

Coverage test **bites** (proven): deleting `pricing.planProName` from `en.json` fails
`messages/coverage.test.ts` (`pricing.planProName missing in en`); restore is byte-identical.

## AC verification

**AC1 — switcher reachable on public surfaces.** Met.

- `components/auth/auth-shell.tsx`: gains only an `absolute right-4 top-4` wrapper hosting
  `<LocaleMenu />`. No `"use client"`, no hooks added → AuthShell stays a server component
  (asserted by `auth-shell.test.ts`).
- `app/[locale]/page.tsx`: new minimal `<header className="bg-pine">` with `Container` →
  `Logo` (left) + `LocaleMenu` (right). Nothing else — no marketing nav / links / CTA / menu.
  The rest of the page diff is pure re-indentation (wrapped `<main>` in a fragment).
- Existing primitive reused: `components/ui/locale-menu.tsx` is imported verbatim, not
  restyled (file not in the diff). It switches via `router.replace(pathname, { locale })`
  from `@/i18n/navigation` (exports `useRouter`/`usePathname` — verified), preserving the path.
  No third switcher written; `LocaleSwitcher` left untouched.

**AC2 — coverage honest, not theatre.** Met.

- `messages/coverage.test.ts` (NEW) enumerates real key arrays for 11 un-pinned namespaces
  (auth + legal already own their tests) and asserts presence in BOTH fr and en.
- Dynamic pricing keys correctly pinned by hand: `PLANS[0]` in `lib/stripe/config.ts` resolves
  `planProName` / `planProPrice` / `planProFeatures` — all three enumerated and present in both
  locales. A literal source-scan would have missed these; the enumerated approach catches them.
- Spot-checked enumerations match the JSON (`dashboard` = greeting/subtitle; `localeSwitcher` =
  label). All enumerated keys verified present in fr.json + en.json.
- `messages/messages.test.ts` (deep parity) unchanged (diff empty).

**AC3 — persistence documented, not rebuilt.** Met.

- `i18n/routing.ts` and `proxy.ts` diffs are empty (verified). Persistence
  (`localePrefix: "as-needed"` + `NEXT_LOCALE` cookie) documented in the coverage-test header,
  no middleware boot test added.

## Discipline / rules

- Finalisation discipline respected: no upstream screen rewritten; auth-shell and landing gain
  only a mounted `<LocaleMenu />`.
- Tokens-only: header is `bg-pine`, `LocaleMenu` trigger is `text-paper/75` (visible on pine);
  `pine`/`paper` are real `@theme` tokens. Design guard green; `check-design-tokens.mjs` unmodified.
- No new npm dependency. `fr.json` / `en.json` untouched (parity holds).
- Tests are source-level structural assertions — consistent with the repo's `environment: node`
  convention (page.test.ts, app-sidebar.test.ts). Minor: structural tests would pass on a
  visually-broken mount, but the mount is a static child render and the pine/text-paper coupling
  is explicitly asserted; acceptable for this finalisation story.
- No ADR contradicted (ADR 001/002 respected).

## Findings

None material. The initial working-tree snapshot showed unrelated `components/brand/logo.*`
changes; these are NOT part of the committed branch diff (`git diff main...feature/s08-i18n`
does not include them) and are out of scope for this review.

Max severity: none
Ship allowed: yes
